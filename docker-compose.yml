version: '3.8'

services:
  app:
    build: .
    restart: always
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgres://dashboard_user:dashboard_password@172.17.0.1:5432/dashboard_financeiro
    network_mode: "bridge"
